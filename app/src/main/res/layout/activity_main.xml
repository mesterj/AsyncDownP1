<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.kite.joco.asyncdownp1.MainActivity">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Progress bar with asynctask próba"
        android:id="@+id/textView" />

    <ProgressBar
        android:layout_width="350dp"
        android:layout_height="50dp"
        style="?android:attr/progressBarStyleHorizontal"
        android:id="@+id/pbLetolt"
        android:layout_below="@+id/textView"
        android:layout_marginTop="5dp"
        android:layout_alignParentStart="true"
        android:layout_alignParentEnd="true" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Letöltés"
        android:id="@+id/btnLetolt"
        android:onClick="letoltClck"
        android:layout_below="@+id/pbLetolt"
        android:layout_alignParentEnd="true"
        android:layout_marginTop="49dp" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Első sor mentve:"
        android:id="@+id/tvElsoment"
        android:layout_below="@+id/btnLetolt"
        android:layout_alignEnd="@+id/textView"
        android:layout_marginTop="93dp" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Utolsó sor mentve:"
        android:id="@+id/tvUtolsoment"
        android:layout_below="@+id/tvElsoment"
        android:layout_centerHorizontal="true" />


</RelativeLayout>
